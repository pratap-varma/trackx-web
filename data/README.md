# TrackX Legacy Data Backup

> [!WARNING]
> The files in this directory (`trackx_db.json`) are **LEGACY BACKUPS ONLY**.
> They are **NOT** used as the runtime database.
> The single authoritative source of truth for persistent TrackX data is **Firebase Firestore** (`users/{uid}/...`).
